# 故障复盘 01（W1 作业：填 1 则真实适配坑，D7 后提交）

> 模板：现象 → 你怎么查的 → 结论 → 以后先查什么  
> **本周不要求实验室复现**，复现放 W2。

| 字段 | 内容 |
|------|------|
| 相关组件 | **K8s / Calico / Redis / Pulsar / Harbor / Kuboard-Spray**（麒麟离线交付） |
| 现象 | 离线装簇后 Redis 反复重启；Pulsar broker FailedMount；Harbor 脚本报 401；模拟断外网后 Spray 8080 不通 |
| 影响 | 中间件无法验收；交付阻塞 |
| 我执行的排查（按顺序列命令或动作） | 1. `kubectl describe` + Pod netns 仅 lo → Sandbox 坏 2. Events 缺 `pulsar-token-asymmetric-key` 3. 匿名 docker pull 成功 vs curl 401 4. 109 查 `ip_forward` + FORWARD 172.17 |
| 根因（一句话） | 多因素：CNI Sandbox 与 kubelet 不同步；JWT Secret 不齐；验收口径用错；模拟离线漏 Docker 转发 |
| 修复动作 | 强制删坏 Pod；补 JWT 密钥 Secret；清 Helm pending；iptables 按角色补 FORWARD；验收改 pull |
| 教训 / 以后第一眼查 | 先看 Events/Secret/Sandbox 网卡；Harbor 先 pull；断外网先分角色；装中间件勿乱 restart kubelet |
| 证据 | 完整 SOP 见 [`cases/kylin-offline-k8s-v1.26/`](cases/kylin-offline-k8s-v1.26/) |

```
交付源码仓：C:\Users\kaihong\Desktop\87\work（VERSION 1.0.14）
案例目录：01-K8s-Troubleshooting/cases/kylin-offline-k8s-v1.26/
```
