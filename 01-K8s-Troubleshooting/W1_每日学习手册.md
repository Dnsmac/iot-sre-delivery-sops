# W1 每日学习手册（跟这份文档做）

| 项 | 说明 |
|----|------|
| 适用人 | 11年Java / 现物联网外包 / K8s 适配过但原理弱 |
| 本周范围 | Pulsar、MySQL三主、Redis、ES、Nacos、MinIO、MetalLB、iot-server、达梦 |
| 每日投入 | 1.5h（严格计时） |
| 本周禁止 | minikube 造故障、啃 CKA 全套、空抄 yaml 不交输出 |
| 有问题 | Cursor 随时发：`【W1-D? 提问】现象/命令/截图` |
| 学完后 | 填本文 **§8 学员提交**，发 `【W1作业提交】`，等导师在 **§9 导师校验** 打勾 |

**辅助文件**（作业阶段才必填）：`生产服务清单.md`、`生产服务-K8s映射表.md`、`W1_学习笔记.md`、`故障复盘_01.md`

---

## §0 总进度（自己打勾）

| 天 | 日期 | 耗时 | 当日自检 | 笔记已写 |
|----|------|------|----------|----------|
| D1 | | | [ ] | [ ] |
| D2 | | | [ ] | [ ] |
| D3 | | | [ ] | [ ] |
| D4 | | | [ ] | [ ] |
| D5 | | | [ ] | [ ] |
| D6 | | | [ ] | [ ] |
| D7 | | | [ ] | [ ] |
| 作业 | | | [ ] 已提交 §8 | |

---

## §1 本周总目标 G1～G6（D7 必须全能答）

| 编号 | 我要搞清楚什么 | 我的一句话答案（D7 填） |
|------|----------------|-------------------------|
| G1 | 设备请求路径 | |
| G2 | 9 组件 Workload + Service | |
| G3 | MetalLB 与 Ingress 区别 | |
| G4 | 有状态集群为何不能乱删 Pod | |
| G5 | iot-server 挂了先查谁 | |
| G6 | 我踩过的坑属于哪一类 | |

---

# D1：K8s 对象对照现网

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 1.1 今日要搞清楚

- [ ] Pod / Deployment / StatefulSet / Service 各是什么
- [ ] 你们环境里：哪几个组件是 Deploy，哪几个是 STS
- [ ] `describe pod` 里 Events、Limits 在哪看

## 1.2 执行步骤（按序，只读）

```bash
kubectl get ns
kubectl get deploy,sts,svc -A
kubectl get deploy,sts,svc -A | findstr /i "pulsar mysql redis es nacos minio iot metallb dameng"
```

任选一个 Pod（记下名字）：

```bash
kubectl get pod <POD名> -n <NS> -o wide
kubectl describe pod <POD名> -n <NS>
kubectl describe svc <SVC名> -n <NS>
```

## 1.3 填写区（必填，可脱敏）

**我查的 Pod / Namespace**：

```
Pod:
NS:
```

**Workload 对照（先填能确认的，不懂写「待确认」）**：

| 组件 | Deploy 还是 STS | NS |
|------|-----------------|-----|
| iot-server | | |
| Pulsar | | |
| MySQL | | |
| Redis | | |
| ES | | |
| Nacos | | |
| MinIO | | |
| MetalLB | | |
| 达梦 | | |

**粘贴一段真实输出（describe pod 的 Events 或 Limits，10～30 行）**：

```
（贴这里）
```

## 1.4 D1 自检（全勾才算 D1 完成）

- [ ] 能说出 Deploy 与 STS 各 1 个组件名
- [ ] 能指出 describe 里 Events 的作用
- [ ] 找到一个 memory limit 并写出数值

## 1.5 今日疑问（可选，复制到 Cursor 提问）

```
【W1-D1 提问】


```

## 1.6 导师批注（学员勿填）

> 

---

# D2：MetalLB 与网络暴露

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 2.1 今日要搞清楚

- [ ] ClusterIP / NodePort / LoadBalancer 区别
- [ ] MetalLB 给哪个 Service 分了 IP
- [ ] iot-server 对外用 LB / NodePort / Ingress 哪一种

## 2.2 执行步骤

```bash
kubectl get svc -A | findstr LoadBalancer
kubectl describe svc <有EXTERNAL-IP的svc> -n <NS>
kubectl get pods -n metallb-system
kubectl get pods -A | findstr /i metallb
```

## 2.3 填写区

**对外暴露链路（文字或箭头）**：

```
外部 → __________ → Service __________ → Pod __________
```

| 问题 | 我的答案 |
|------|----------|
| iot-server 对外方式 | LB / NodePort / Ingress（圈一个） |
| MetalLB IP 网段或描述 | |
| Ingress 我们有没有用 IoT 业务 | 有 / 无 |

**粘贴：`kubectl get svc` 中含 EXTERNAL-IP 的一行**：

```
（贴这里）
```

## 2.4 D2 自检

- [ ] 能画 L4：外部 IP → Service → Pod
- [ ] 说清 MetalLB 解决什么问题（一句话）
- [ ] 说清 Ingress 和 LB 层级差（L7 vs L4）

## 2.5 今日疑问

```
【W1-D2 提问】


```

## 2.6 导师批注

> 

---

# D3：Pulsar 集群

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 3.1 今日要搞清楚

- [ ] Pulsar 在 K8s 是 STS 还是 Deploy
- [ ] 数据是否在 PVC
- [ ] 设备/iot-server 是否走 Pulsar（看配置确认）

## 3.2 执行步骤

```bash
kubectl get sts,svc,pvc -n <Pulsar的NS>
kubectl get pods -n <Pulsar的NS> -o wide
kubectl describe sts <sts名> -n <Pulsar的NS>
```

## 3.3 填写区

| 项 | 答案 |
|----|------|
| Pulsar NS | |
| STS 名称 | |
| Pod 大约几个 | |
| PVC 有/无 | |
| iot-server 是否用 Pulsar | 是/否/待确认 |
| 适配踩坑关键词（1～3 个词） | |

**粘贴：get pods 或 describe sts 片段**：

```
（贴这里）
```

## 3.4 D3 自检

- [ ] 能说出 Broker / BookKeeper 各负责什么（大白话）
- [ ] 知道消息积压时先查哪一层（Broker/Bookie/消费端）

## 3.5 今日疑问

```
【W1-D3 提问】


```

## 3.6 导师批注

> 

---

# D4：MySQL 三主 + 达梦

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 4.1 今日要搞清楚

- [ ] MySQL **真实拓扑名词**（三主？MGR？一主多从？——必须现网确认，不能猜）
- [ ] 达梦：单点/主备/集群
- [ ] iot-server 连 MySQL、达梦，或两个都连

## 4.2 执行步骤

```bash
kubectl get sts,svc,pvc -n <MySQL的NS>
kubectl describe sts <mysql-sts> -n <NS>
kubectl get cm,secret -n <iot或nacos的NS> | findstr /i "jdbc mysql dameng datasource"
```

## 4.3 填写区

| 项 | 答案 |
|----|------|
| MySQL 真实拓扑（自己查到的名词） | |
| MySQL STS/SVC 名 | |
| 达梦部署位置 | 集群内 / 集群外 |
| 达梦拓扑 | |
| iot-server 数据源 | MySQL / 达梦 / 两者 |
| 适配踩坑关键词 | |

**数据流一句话**：iot-server → __________ → __________

## 4.4 D4 自检

- [ ] 没有笼统写「三主」而没有证据
- [ ] 能说出连接池打满或主从切换时业务现象（回忆即可）

## 4.5 今日疑问

```
【W1-D4 提问】


```

## 4.6 导师批注

> 

---

# D5：Redis 集群 + ES 集群

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 5.1 今日要搞清楚

- [ ] Redis：集群模式、节点数、Service 名
- [ ] ES：节点数、memory limit、用途（日志/检索/两者）
- [ ] 内存或磁盘满时各会出现什么现象

## 5.2 执行步骤

```bash
kubectl get sts,deploy,svc -n <Redis的NS>
kubectl get sts,svc -n <ES的NS>
kubectl describe pod <es-pod> -n <NS>
```

## 5.3 填写区

| 组件 | NS | Workload | 节点/副本约数 | memory limit 约 | 用途/踩坑词 |
|------|-----|----------|---------------|-----------------|-------------|
| Redis | | | | | |
| ES | | | | | |

## 5.4 D5 自检

- [ ] Redis 与 ES 各 1 个「满了会怎样」
- [ ] 能说出 ES 堆内存和 limits 的关系（一句）

## 5.5 今日疑问

```
【W1-D5 提问】


```

## 5.6 导师批注

> 

---

# D6：Nacos + MinIO + iot-server 启动链

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 6.1 今日要搞清楚

- [ ] iot-server 启动依赖顺序（Nacos、DB、Redis、Pulsar…以你项目为准）
- [ ] readiness 失败时流量会不会进 Pod
- [ ] MinIO 存什么、bucket 是否知道

## 6.2 执行步骤

```bash
kubectl get deploy -n <iot的NS>
kubectl get deploy <iot-server名> -n <NS> -o yaml
# 只看：env、livenessProbe、readinessProbe、resources、volumeMounts
kubectl get deploy,svc -n <Nacos的NS>
kubectl get deploy,svc -n <MinIO的NS>
```

## 6.3 填写区

**iot-server 依赖（至少 3 个，写服务名）**：

1.
2.
3.

| 项 | 答案 |
|----|------|
| liveness 探针类型/路径 | |
| readiness 探针类型/路径 | |
| CPU/内存 requests | |
| CPU/内存 limits | |
| MinIO 用途 | |
| Nacos 导致过全服务重启？ | 是/否/不记得 |

## 6.4 D6 自检

- [ ] 能口述：iot-server 启动失败排查顺序（≥3 步）
- [ ] 知道 Nacos 配置与注册的区别（各一句）

## 6.5 今日疑问

```
【W1-D6 提问】


```

## 6.6 导师批注

> 

---

# D7：串讲 + 总自检 + 开始作业

**今日日期**：________　**实际耗时**：________　**状态**：[ ] 完成

## 7.1 今日要搞清楚

- [ ] G1～G6 全部能答（回填 §1 表）
- [ ] 9 组件各一句：Workload + 怎么访问
- [ ] 适配最难的 1 个组件 + 原因

## 7.2 执行步骤

1. 对着 `生产服务清单.md` 架构图 **自己讲 3 分钟**（建议录音）
2. 填 `生产服务-K8s映射表.md` 至少 **9 行**
3. **无 AI 30min**：手写下面 4 条命令的真实输出（不许粘贴 AI 生成的）

```bash
kubectl get pods -n <NS>
kubectl describe pod <POD> -n <NS>
kubectl logs <POD> -n <NS> --tail=20
kubectl get events -n <NS> --sort-by='.lastTimestamp'
```

## 7.3 填写区

**最难适配的组件**：________　**原因（1 句）**：________

**3 分钟串讲稿（提纲 5 行即可）**：

```
1.
2.
3.
4.
5.
```

**无 AI 手写命令输出（贴终端原文）**：

```
（贴这里）
```

## 7.4 D7 总自检（全勾才能做 §8 作业）

- [ ] §1 的 G1～G6 已填
- [ ] §0 的 D1～D7 当日自检全勾
- [ ] 9 组件 Workload 表（D1）无整列空白
- [ ] 完成无 AI 30min 四条命令
- [ ] `生产服务-K8s映射表.md` ≥9 行

## 7.5 今日疑问

```
【W1-D7 提问】


```

## 7.6 导师批注

> 

---

## §8 学员提交（D7 完成后填写）

**提交日期**：________  
**Git 提交 hash（如有）**：________  
**GitHub 仓库链接（如有）**：________  

### 8.1 一并提交的文件清单

- [ ] 本文件 `W1_每日学习手册.md`（D1～D7 填写区已填）
- [ ] `生产服务清单.md`（9 项 + 10～15 行业务服务）
- [ ] `生产服务-K8s映射表.md`（9 行全填）
- [ ] `W1_学习笔记.md`（新建，汇总每日要点）
- [ ] `故障复盘_01.md`（1 则真实适配坑）
- [ ] 3 分钟串讲：录音路径 / 或写在本手册 D7

### 8.2 学员自评（诚实勾选）

| 项 | 是 | 否 |
|----|----|-----|
| 所有 kubectl 输出来自自己集群 | [ ] | [ ] |
| 无编造 MySQL 拓扑名词 | [ ] | [ ] |
| 已做无 AI 30min | [ ] | [ ] |
| 公司敏感信息已脱敏 | [ ] | [ ] |

### 8.3 请导师校验（复制到 Cursor 发送）

```text
【W1作业提交】
- 手册：01-K8s-Troubleshooting/W1_每日学习手册.md
- 清单：生产服务清单.md
- 映射表：生产服务-K8s映射表.md
- 笔记：W1_学习笔记.md
- 故障：故障复盘_01.md
自评：§8.2 全勾 / 有未勾项请说明 ________
```

---

## §9 导师校验（学员提交后由导师填写）

> **状态**：[ ] 未提交　[ ] 待校验　[ ] 通过解锁 W2　[ ] 驳回补交

**校验日期**：________

| 序号 | 验收项 | 通过 | 驳回原因 |
|------|--------|------|----------|
| 1 | D1～D7 填写区非空、非敷衍 | [ ] | |
| 2 | 9 组件 Workload/NS 基本正确 | [ ] | |
| 3 | MetalLB / iot 暴露链路说得通 | [ ] | |
| 4 | MySQL 拓扑有证据非瞎写 | [ ] | |
| 5 | 含真实 kubectl 输出片段 | [ ] | |
| 6 | `生产服务-K8s映射表.md` 9 行全填 | [ ] | |
| 7 | `故障复盘_01.md` 现象→排查→结论完整 | [ ] | |
| 8 | G1～G6 面试一句话能答 | [ ] | |
| 9 | 无 AI 30min 有终端原文 | [ ] | |

**导师总评**：

```
（通过 / 驳回 + 3 条以内修改意见）


```

**驳回时只补这些（不超过 2 项）**：

1.
2.

**解锁 W2 条件**：上表 1～9 全通过。

---

## 附录：卡住 >15min

| 情况 | 做法 |
|------|------|
| 无 kubectl 权限 | 要只读 kubeconfig 或 describe 截图，贴进当日填写区 |
| 找不到 NS | `kubectl get pods -A \| findstr 关键字` |
| 当天超时 | 砍扩展阅读，保当日「自检」勾上 |
| 任意卡点 | Cursor：`【W1-D? 提问】` + 已尝试 2 步 |
