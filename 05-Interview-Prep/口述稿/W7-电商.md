# W7 电商口述稿（ERP 并发正确性 · 90 秒）

> 定位：ERP/电商是副战场，用来证明 Java 后端业务并发信用；主线仍回到 IoT。
> 事实边界以 [`../数字与边界-唯一索引.md`](../数字与边界-唯一索引.md) §5 和 [`../stories/电商-高并发模块.md`](../stories/电商-高并发模块.md) 为准。

---

## 边界提醒

- 只讲历史材料范围：300+ 商户、日峰值约 3000 万、分布式锁、幂等、唯一约束、状态机四类机制。
- 未知必须明说：接口/回调/消费端/状态流具体落点、表结构、SQL、结果和四件套组合待本人核对。
- 不主动包装成秒杀 owner；秒杀、库存分片、大促压测只能作为官方变体回答。
- 讲完 ERP 后要自然带回 IoT 主线：15W 接入、operatorCache OOM、ES NFS 写慢。

---

## 稿（可照着录）

电商/ERP 我只按当前可核对边界讲。历史材料记录这个多商户系统有 **300+ 商户**、**日峰值约 3000 万**，并提到 **分布式锁、幂等、唯一约束、状态机**四类机制。

但这些机制具体落在哪个接口、回调、消费端或状态流，怎样组合，唯一键和表结构是什么，目前还没由我本人核对。“零超卖”“零重复扣款”也没有当前证据，所以我不把通用方案说成项目实现，不主动深讲。

如果面试官继续追，我会说明待补证据是接口定义、唯一约束、状态流转日志和结果记录；当前优先转到有证据的 IoT 15W 接入、OOM 定位和 ES 写慢案例。

---

## 30 秒兜底版

```text
ERP 我只讲已证边界：300+ 商户、日峰值约 3000 万。
历史材料记录分布式锁、幂等、唯一约束、状态机四类机制。
具体接口、回调、消费端、状态流落点及结果待本人核对，当前不主动深讲。
如果您关注 IoT，我可以展开有证据的 15W 接入、OOM 定位和 ES 写慢。
```

---

## 高频追问

1. 幂等 key 用什么字段，谁生成？
2. 为什么只有 Redis 锁不够？
3. 唯一键冲突时返回成功、失败还是查询原单？
4. 状态机通用上如何限制非法流转？项目状态流是否已核对？
5. 如果要证明没有重复，需要哪些日志、SQL 或对账证据？

---

## 反抛句

> 这段 ERP 历史材料的具体链路还待本人核对，当前不主动深讲。如果您更关注 IoT 岗位，我可以展开有证据的 15W 长连接、OOM 定位和 ES 写慢。

---

## 录音记录表

| 日期 | 时长 | 流畅度（1-5） | 卡壳点 | 下一步 |
|------|------|---------------|--------|----------|
| | | | | |
